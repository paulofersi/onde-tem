import app from "@react-native-firebase/app";
import auth from "@react-native-firebase/auth";

// O Firebase já está inicializado automaticamente pelo react-native-firebase
// Não precisamos de configuração manual

export { app, auth };
export default app;
